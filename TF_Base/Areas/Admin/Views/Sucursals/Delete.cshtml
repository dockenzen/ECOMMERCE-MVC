@model TF_Base.Models.Sucursal

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Sucursal</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            
        </dt>

        <dt>
           
        </dt>

        <dt>
            
        </dt>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>


<div class="col-lg-4">
    <div class="card">
        <div class="card-header">
            <h6 class="text-uppercase mb-0">Eliminar Sucursal</h6>
        </div>
        <div class="card-body">
            <p>Seguro que desea eliminar este registro ?</p>
            <table class="table table-striped table-hover card-text">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.descripcion)</th>
                        <th>@Html.DisplayNameFor(model => model.email)</th>
                        <th>@Html.DisplayNameFor(model => model.telefono)</th>
                        <th>@Html.DisplayNameFor(model => model.Direccion.direccion1)</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>@Html.DisplayFor(model => model.descripcion)</td>
                        <td>@Html.DisplayFor(model => model.email)</td>
                        <td>@Html.DisplayFor(model => model.telefono)</td>
                        <td>@Html.DisplayFor(model => model.Direccion.direccion1)</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="card-body text-center">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Eliminar" class="btn btn-primary" /> |
                    @Html.ActionLink("Volver a la lista", "Index")
                </div>
            }
        </div>
    </div>
</div>