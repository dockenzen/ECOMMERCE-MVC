@model TF_Base.Models.CostoEnvio

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}

<div class="col-lg-4">
    <div class="card">
        <div class="card-header">
            <h6 class="text-uppercase mb-0">Eliminar Costo</h6>
        </div>
        <div class="card-body">
            <p>Seguro que desea eliminar este registro ?</p>
            <table class="table table-striped table-hover card-text">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Importe)</th>
                        <th>@Html.DisplayNameFor(model => model.Localidad.localidad1)</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>@Html.DisplayFor(model => model.Localidad.localidad1)</td>
                        <td>@Html.DisplayFor(model => model.Importe)</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="card-body text-center">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Eliminar" class="btn btn-primary" /> |
                    @Html.ActionLink("Volver a la lista", "Index")
                </div>
            }
        </div>
    </div>
</div>