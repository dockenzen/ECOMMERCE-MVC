@model IEnumerable<TF_Base.Models.CostoEnvio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}


<div class="col-lg-10">
    <p>
        @Html.ActionLink("Crear Nuevo", "Create", "CostoEnvios", null, new { @class = "btn btn-primary" })
    </p>
</div>

<div class="col-lg-7">
    <div class="card">
        <div class="card-header">
            <h6 class="text-uppercase mb-0">Costos de Envio</h6>
        </div>
        <div class="card-body">
            <table class="table table-striped table-hover card-text">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Importe)</th>
                        <th>@Html.DisplayNameFor(model => model.Localidad.localidad1)</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.Importe)</td>
                            <td>@Html.DisplayFor(modelItem => item.Localidad.localidad1)</td>
                            <td>
                                @Html.ActionLink("Editar", "Edit", new { id = item.idCostoEnvio }) |
                                @*@Html.ActionLink("Details", "Details", new { id = item.idCostoEnvio }) |*@
                                @Html.ActionLink("Eliminar", "Delete", new { id = item.idCostoEnvio })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
